export const SERVER_HOSTNAME = process.env.HOSTNAME;
export const SERVER_PORT = process.env.SERVER_PORT;

export const SERVER = {
  hostname: SERVER_HOSTNAME,
  port: SERVER_PORT,
};

export const serverConfig = {
  server: SERVER,
};

export default serverConfig;
